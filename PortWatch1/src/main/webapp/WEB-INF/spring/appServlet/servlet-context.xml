<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="
        http://www.springframework.org/schema/mvc
        https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- ============================================================
         위 치 : src/main/webapp/WEB-INF/spring/appServlet/servlet-context.xml
         ============================================================ -->

    <!-- Enables the Spring MVC @Controller programming model -->
    <annotation-driven />

    <!-- Handles HTTP GET requests for /resources/** -->
    <resources mapping="/resources/**" location="/resources/" />

    <!-- ViewResolver: /WEB-INF/views/*.jsp -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>

    <!-- Controller 스캔 -->
    <context:component-scan base-package="com.portwatch.controller" />

    <!-- ============================================================
         Multipart Resolver (파일 업로드)
         ※ CommonsMultipartResolver 는 commons-fileupload JAR 필요
            → 없으면 BeanCreationException 발생
            → StandardServletMultipartResolver 사용 (Servlet 3.0 기본 제공)
            ※ web.xml 의 DispatcherServlet 에 multipart-config 태그 추가 필요
         ============================================================ -->
    <beans:bean id="multipartResolver"
        class="org.springframework.web.multipart.support.StandardServletMultipartResolver"/>

    <!-- JSON Converter (Jackson) -->
    <beans:bean id="jsonHttpMessageConverter"
        class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"/>

</beans:beans>
