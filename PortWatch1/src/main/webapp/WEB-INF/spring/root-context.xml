<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:task="http://www.springframework.org/schema/task"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx.xsd
        http://www.springframework.org/schema/task
        http://www.springframework.org/schema/task/spring-task.xsd
        http://mybatis.org/schema/mybatis-spring
        http://mybatis.org/schema/mybatis-spring-1.2.xsd">

    <!-- ============================================================
         ✅ 로컬 STS 콘솔 오류 해결 포인트
         ConflictingBeanDefinitionException: 'memberMapper' 충돌

         원인: persistence.MemberMapper(bean="memberMapper") 와
               mapper.MemberMapper(bean="memberMapper") 이름 중복

         해결책:
           1. persistence.MemberMapper.java → 완전 삭제
           2. persistence.MemberDAO.java    → @Mapper("memberDAO") 사용
           3. mybatis-spring:scan → mapper 패키지만 스캔
              (persistence 는 @Mapper 어노테이션으로 자동 등록)

         ※ annotationClass 속성은 mybatis-spring-1.2.xsd 미지원 → 사용 불가
         ============================================================ -->

    <!--
        ══════════════════════════════════════════════════════════════
        HikariCP DataSource - AWS RDS Aurora MySQL 연결
        (HikariCP 3.4.5 + MySQL Connector/J 8.x + Aurora MySQL)
        ══════════════════════════════════════════════════════════════

        ⚠️  [현재 오류 2단계 분석] 2026-04-02 스크린샷 기준
        ─────────────────────────────────────────────────────────────
        1단계 (해결됨): HikariPool-1 timed out after 30000ms
           원인: setenv.sh 권한 600 → IPv4 미적용 → IPv6 DNS 지연
           해결: sudo tee /opt/tomcat/bin/setenv.sh + chmod 755

        2단계 (현재): CommunicationsException: Communications link failure
                      + ConnectException: Connection timed out
           원인: Aurora로 전환 시 엔드포인트가 변경되었으나
                 root-context.xml은 구 MySQL 엔드포인트 사용 중
           해결: 아래 jdbcUrl을 Aurora 클러스터 Writer 엔드포인트로 변경
        ─────────────────────────────────────────────────────────────

        ✅ Aurora 엔드포인트 확인 방법:
           AWS 콘솔 → RDS → 데이터베이스 → [클러스터명] 클릭
           → 연결 및 보안 탭 → 엔드포인트 섹션
           → "쓰기" 유형의 엔드포인트 주소 복사
           형식: [클러스터명].cluster-[id].ap-northeast-2.rds.amazonaws.com

        ✅ Aurora MySQL vs 일반 MySQL 드라이버 차이:
           - JDBC URL 프리픽스는 동일: jdbc:mysql://
           - MySQL Connector/J 8.x가 Aurora를 자동 감지
           - useSSL=false&sslMode=DISABLED 동일하게 적용

        ✅ AWS EC2 IPv4 강제 (EC2 setenv.sh 필수):
           sudo tee /opt/tomcat/bin/setenv.sh << 'EOF'
           #!/bin/bash
           export JAVA_OPTS="-Djava.net.preferIPv4Stack=true -Xms256m -Xmx512m"
           EOF
           sudo chmod 755 /opt/tomcat/bin/setenv.sh

        ✅ HikariCP 3.4.5 + Aurora 권장 설정:
           - connectTimeout=5000 (Aurora는 빠른 장애조치 지원, 5초면 충분)
           - socketTimeout=60000 (쿼리 최대 60초)
           - connectionTimeout=30000 (풀 대기 최대 30초)
           - maxLifetime=180000 (AWS NAT 4분 타임아웃 전에 갱신)
           - idleTimeout=120000 (유휴 연결 2분 후 반환)
        ══════════════════════════════════════════════════════════════
    -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
        <!--
            ⚠️  [필수 수정] Aurora 클러스터 Writer 엔드포인트로 변경하세요!
            ─────────────────────────────────────────────────────────────
            구 MySQL 엔드포인트 (삭제/변경됨):
              portwatch-db-seoul.cpggwqm1goo3.ap-northeast-2.rds.amazonaws.com

            신규 Aurora 엔드포인트 예시:
              [클러스터명].cluster-[id].ap-northeast-2.rds.amazonaws.com

            AWS 콘솔에서 확인 후 아래 jdbcUrl의 호스트명을 교체하세요.
            ─────────────────────────────────────────────────────────────
            ✅ connectTimeout=5000  (Aurora 장애조치 고려, 5초)
               socketTimeout=60000  (쿼리 실행 최대 60초)
               sslMode=DISABLED     (Aurora도 내부 통신은 SSL 불필요)
               enabledTLSProtocols=TLSv1.2 (Aurora SSL 끄기 보조)
        -->
        <property name="jdbcUrl"
    value="jdbc:mysql://portwatch-db-seoul.cpggwqmigoo3.ap-northeast-2.rds.amazonaws.com:3306/portwatch
           ?useSSL=false
           &amp;sslMode=DISABLED
           &amp;serverTimezone=Asia/Seoul
           &amp;characterEncoding=UTF-8
           &amp;allowPublicKeyRetrieval=true
           &amp;connectTimeout=5000
           &amp;socketTimeout=60000
           &amp;autoReconnect=true
           &amp;failOverReadOnly=false"/>
        <property name="username" value="admin"/>
        <property name="password" value="portwatch2026!!abcd"/>
        <!--
            ✅ Aurora 권장 풀 크기: db.t3.medium → 최대 10개 적절
               Aurora는 연결 재사용 효율이 높아 minimumIdle=2 권장
        -->
        <property name="maximumPoolSize" value="10"/>
        <property name="minimumIdle" value="2"/>
        <!--
            ✅ connectionTimeout=30000 (30초): 풀에서 연결 대기 최대 시간
               반드시 connectTimeout(5000) 보다 커야 함
        -->
        <property name="connectionTimeout" value="30000"/>
        <property name="idleTimeout" value="120000"/>
        <property name="maxLifetime" value="180000"/>
        <property name="connectionTestQuery" value="SELECT 1"/>
        <!-- ✅ -1: 기동 시 DB 연결 실패해도 Tomcat 기동 허용 -->
        <property name="initializationFailTimeout" value="-1"/>
    </bean>

    <!-- MyBatis SqlSessionFactory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:/mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath:/mappers/**/*.xml"/>
        <property name="typeAliasesPackage" value="com.portwatch.domain"/>
    </bean>

    <bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate">
        <constructor-arg ref="sqlSessionFactory"/>
    </bean>

    <!-- ✅ 핵심 수정: persistence.MemberMapper 삭제 후 두 패키지 스캔
         각 인터페이스에 @Mapper("고유이름") 으로 충돌 방지 -->
    <mybatis-spring:scan base-package="com.portwatch.mapper,com.portwatch.persistence"/>

    <!-- Transaction Manager -->
    <bean id="transactionManager"
        class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- BCrypt Password Encoder -->
    <bean id="passwordEncoder"
        class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <!-- ✅ 핵심: @Scheduled 어노테이션 활성화 -->
    <!-- 이 설정이 없으면 @Scheduled가 코드에 있어도 스케줄이 실행되지 않음 -->
    <task:annotation-driven scheduler="taskScheduler"/>

    <!-- 스케줄러 스레드 풀 (최대 5개 동시 실행) -->
    <task:scheduler id="taskScheduler" pool-size="5"/>

    <!-- Service & Scheduler & Config 컴포넌트 스캔 -->
    <context:component-scan base-package="com.portwatch.service"/>
    <context:component-scan base-package="com.portwatch.scheduler"/>
    <context:component-scan base-package="com.portwatch.config"/>
    <context:annotation-config/>

</beans>
