<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PORTFOLIO MAPPER - 완벽 수정 (avg_price 사용)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 핵심 수정:
1. purchase_price → avg_price로 되돌림
2. PORTFOLIO 테이블 실제 컬럼명과 일치

실제 테이블 구조:
- portfolio_id
- member_id
- stock_id
- quantity
- avg_price (NOT purchase_price!)
- purchase_date
- created_at
- updated_at
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-->

<mapper namespace="com.portwatch.persistence.PortfolioDAO">
    
    <!-- ✅ 회원별 포트폴리오 조회 (stock 정보 포함) -->
    <select id="selectPortfolioByMember" parameterType="string" resultType="PortfolioVO">
        SELECT 
            p.portfolio_id,
            p.member_id,
            p.stock_id,
            p.quantity,
            p.avg_price AS purchase_price,
            p.purchase_date,
            p.created_at,
            p.updated_at,
            s.stock_code,
            s.stock_name,
            s.current_price,
            s.market_type,
            s.country
        FROM portfolio p
        LEFT JOIN stock s ON p.stock_id = s.stock_id
        WHERE p.member_id = #{memberId}
        ORDER BY p.created_at DESC
    </select>
    
    <!-- ✅ 포트폴리오 ID로 조회 -->
    <select id="selectPortfolioById" parameterType="long" resultType="PortfolioVO">
        SELECT 
            p.portfolio_id,
            p.member_id,
            p.stock_id,
            p.quantity,
            p.avg_price AS purchase_price,
            p.purchase_date,
            p.created_at,
            p.updated_at,
            s.stock_code,
            s.stock_name,
            s.current_price,
            s.market_type,
            s.country
        FROM portfolio p
        LEFT JOIN stock s ON p.stock_id = s.stock_id
        WHERE p.portfolio_id = #{portfolioId}
    </select>
    
    <!-- ✅ 회원 + 주식으로 포트폴리오 조회 -->
    <select id="selectByMemberAndStock" parameterType="map" resultType="PortfolioVO">
        SELECT 
            portfolio_id,
            member_id,
            stock_id,
            quantity,
            avg_price AS purchase_price,
            purchase_date,
            created_at,
            updated_at
        FROM portfolio
        WHERE member_id = #{memberId} AND stock_id = #{stockId}
    </select>
    
    <!-- ✅ 포트폴리오 등록 -->
    <insert id="insertPortfolio" parameterType="PortfolioVO" useGeneratedKeys="true" keyProperty="portfolioId">
        INSERT INTO portfolio (
            member_id,
            stock_id,
            quantity,
            avg_price,
            purchase_date,
            created_at,
            updated_at
        ) VALUES (
            #{memberId},
            #{stockId},
            #{quantity},
            #{purchasePrice},
            #{purchaseDate},
            NOW(),
            NOW()
        )
    </insert>
    
    <!-- ✅ 포트폴리오 수정 -->
    <update id="updatePortfolio" parameterType="PortfolioVO">
        UPDATE portfolio
        SET 
            quantity = #{quantity},
            avg_price = #{purchasePrice},
            updated_at = NOW()
        WHERE portfolio_id = #{portfolioId}
    </update>
    
    <!-- ✅ 포트폴리오 삭제 -->
    <delete id="deletePortfolio" parameterType="long">
        DELETE FROM portfolio
        WHERE portfolio_id = #{portfolioId}
    </delete>
    
    <!-- ✅ 전체 포트폴리오 조회 -->
    <select id="selectAllPortfolio" resultType="PortfolioVO">
        SELECT 
            p.portfolio_id,
            p.member_id,
            p.stock_id,
            p.quantity,
            p.avg_price AS purchase_price,
            p.purchase_date,
            p.created_at,
            p.updated_at,
            s.stock_code,
            s.stock_name,
            s.current_price,
            s.market_type,
            s.country
        FROM portfolio p
        LEFT JOIN stock s ON p.stock_id = s.stock_id
        ORDER BY p.created_at DESC
    </select>
    
    <!-- ✅ 포트폴리오 수량 업데이트 -->
    <update id="updateQuantity" parameterType="map">
        UPDATE portfolio
        SET 
            quantity = #{quantity},
            updated_at = NOW()
        WHERE portfolio_id = #{portfolioId}
    </update>
    
    <!-- ✅ 포트폴리오 평균 단가 업데이트 -->
    <update id="updateAvgPrice" parameterType="map">
        UPDATE portfolio
        SET 
            avg_price = #{avgPrice},
            updated_at = NOW()
        WHERE portfolio_id = #{portfolioId}
    </update>
    
    <!-- ✅ 특정 주식의 총 수량 조회 -->
    <select id="getTotalQuantityByStock" parameterType="map" resultType="int">
        SELECT COALESCE(SUM(quantity), 0)
        FROM portfolio
        WHERE member_id = #{memberId} AND stock_id = #{stockId}
    </select>
    
    <!-- ✅ 회원의 포트폴리오 개수 조회 -->
    <select id="getPortfolioCount" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM portfolio
        WHERE member_id = #{memberId}
    </select>
    
    <!-- ✅ 회원의 총 투자금액 조회 -->
    <select id="getTotalInvestment" parameterType="string" resultType="double">
        SELECT COALESCE(SUM(quantity * avg_price), 0)
        FROM portfolio
        WHERE member_id = #{memberId}
    </select>
    
</mapper>
