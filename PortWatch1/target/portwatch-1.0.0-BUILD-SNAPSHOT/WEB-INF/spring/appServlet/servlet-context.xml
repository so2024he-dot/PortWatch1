<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        servlet-context.xml - 최종 수정본

        ✅ 핵심 수정:
          AdminNewsUpdateController를 component-scan에서 제외
          또는 NewsScheduler.java를 프로젝트에 추가하면 제외 불필요

        [선택 1] NewsScheduler.java를 추가한 경우:
          아래 exclude-filter 블록을 주석 처리하면 됨

        [선택 2] NewsScheduler.java 추가가 어려운 경우:
          AdminNewsUpdateController를 component-scan에서 제외
    -->

    <annotation-driven/>
    <resources mapping="/resources/**" location="/resources/"/>

    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/"/>
        <beans:property name="suffix" value=".jsp"/>
        <beans:property name="order"  value="1"/>
    </beans:bean>

    <!--
        ✅ 선택 1: NewsScheduler.java를 프로젝트에 추가한 경우
                   (권장 - 뉴스 크롤링 기능 유지)
                   아래 excludeFilter 없이 이 설정 사용

        ✅ 선택 2: NewsScheduler.java 없이 즉시 해결하려면
                   excludeFilter 블록 사용
    -->

    <!-- 선택 1: NewsScheduler.java 추가 후 사용 (excludeFilter 없음) -->
    <context:component-scan base-package="com.portwatch.controller"/>

    <!--
    선택 2: NewsScheduler.java 없이 즉시 해결
    (선택1 위의 component-scan을 주석 처리하고 아래를 활성화)

    <context:component-scan base-package="com.portwatch.controller">
        <context:exclude-filter type="assignable"
            expression="com.portwatch.controller.AdminNewsUpdateController"/>
    </context:component-scan>
    -->

</beans:beans>
