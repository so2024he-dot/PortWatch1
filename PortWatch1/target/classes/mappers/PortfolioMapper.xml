<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PORTFOLIO MAPPER - 완벽 수정
Spring 5.0.7 + MySQL 8.0.33 + MyBatis 3.4.6

수정 내용:
- avg_price → purchase_price로 변경
- 모든 테이블명 소문자 사용 (stock, portfolio)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-->

<mapper namespace="com.portwatch.persistence.PortfolioDAO">
    
    <!-- ✅ 회원별 포트폴리오 조회 (stock 정보 포함) -->
    <select id="selectPortfolioByMember" parameterType="string" resultType="PortfolioVO">
        SELECT 
            p.portfolio_id,
            p.member_id,
            p.stock_id,
            p.quantity,
            p.purchase_price,
            p.purchase_date,
            p.created_at,
            p.updated_at,
            s.stock_code,
            s.stock_name,
            s.current_price,
            s.market_type,
            s.country
        FROM portfolio p
        LEFT JOIN stock s ON p.stock_id = s.stock_id
        WHERE p.member_id = #{memberId}
        ORDER BY p.created_at DESC
    </select>
    
    <!-- ✅ 포트폴리오 ID로 조회 -->
    <select id="selectPortfolioById" parameterType="long" resultType="PortfolioVO">
        SELECT 
            p.portfolio_id,
            p.member_id,
            p.stock_id,
            p.quantity,
            p.purchase_price,
            p.purchase_date,
            p.created_at,
            p.updated_at,
            s.stock_code,
            s.stock_name,
            s.current_price,
            s.market_type,
            s.country
        FROM portfolio p
        LEFT JOIN stock s ON p.stock_id = s.stock_id
        WHERE p.portfolio_id = #{portfolioId}
    </select>
    
    <!-- ✅ 회원 + 주식으로 포트폴리오 조회 -->
    <select id="selectByMemberAndStock" parameterType="map" resultType="PortfolioVO">
        SELECT 
            portfolio_id,
            member_id,
            stock_id,
            quantity,
            purchase_price,
            purchase_date,
            created_at,
            updated_at
        FROM portfolio
        WHERE member_id = #{memberId} AND stock_id = #{stockId}
    </select>
    
    <!-- ✅ 포트폴리오 등록 -->
    <insert id="insertPortfolio" parameterType="PortfolioVO" useGeneratedKeys="true" keyProperty="portfolioId">
        INSERT INTO portfolio (
            member_id,
            stock_id,
            quantity,
            purchase_price,
            purchase_date,
            created_at,
            updated_at
        ) VALUES (
            #{memberId},
            #{stockId},
            #{quantity},
            #{purchasePrice},
            #{purchaseDate},
            NOW(),
            NOW()
        )
    </insert>
    
    <!-- ✅ 포트폴리오 수정 -->
    <update id="updatePortfolio" parameterType="PortfolioVO">
        UPDATE portfolio
        SET 
            quantity = #{quantity},
            purchase_price = #{purchasePrice},
            purchase_date = #{purchaseDate},
            updated_at = NOW()
        WHERE portfolio_id = #{portfolioId}
    </update>
    
    <!-- ✅ 포트폴리오 삭제 -->
    <delete id="deletePortfolio" parameterType="long">
        DELETE FROM portfolio
        WHERE portfolio_id = #{portfolioId}
    </delete>
    
    <!-- ✅ 총 평가액 조회 -->
    <select id="getTotalValue" parameterType="string" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(p.quantity * s.current_price), 0)
        FROM portfolio p
        LEFT JOIN stock s ON p.stock_id = s.stock_id
        WHERE p.member_id = #{memberId}
    </select>
    
    <!-- ✅ 총 투자금액 조회 -->
    <select id="getTotalInvestment" parameterType="string" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(p.quantity * p.purchase_price), 0)
        FROM portfolio
        WHERE member_id = #{memberId}
    </select>
    
    <!-- ✅ 포트폴리오 개수 조회 -->
    <select id="countPortfolio" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM portfolio
        WHERE member_id = #{memberId}
    </select>
    
    <!-- ✅ 모든 포트폴리오 조회 (관리자용) -->
    <select id="selectAllPortfolio" resultType="PortfolioVO">
        SELECT 
            p.portfolio_id,
            p.member_id,
            p.stock_id,
            p.quantity,
            p.purchase_price,
            p.purchase_date,
            p.created_at,
            p.updated_at,
            s.stock_code,
            s.stock_name,
            s.current_price,
            s.market_type,
            s.country
        FROM portfolio p
        LEFT JOIN stock s ON p.stock_id = s.stock_id
        ORDER BY p.created_at DESC
    </select>
    
</mapper>
